---
import Base from "../../layouts/Base.astro";
const t = {
  pageTitle: "Join Our Team",
  heroTitle: "Join Our Team",
  heroSub: "Grow With Us, Shape the Future",
  sections: [
    {
      title: "Career",
      subtitle: "Grow With Us, Shape the Future",
      content: "VIEWCO Engineering Co., Ltd., founded in 1984, is a leader in Hong Kong's construction industry. We have not only witnessed Hong Kong's historical transformation but have also left an indelible mark in the building and engineering sector."
    },
    {
      title: "Our Expertise",
      subtitle: "Areas of Expertise",
      content: null,
      subsections: [
        { title: "Air-Conditioning Systems", text: "We have extensive experience in delivering efficient and reliable air-conditioning and ventilation solutions to our clients." },
        { title: "Ventilation Systems", text: "From design to installation, our expert team ensures your exhaust systems operate optimally, maintaining excellent indoor air quality." },
        { title: "Alteration & Maintenance", text: "We assist clients with building alterations and maintenance of existing systems to ensure long-term operational reliability." }
      ]
    },
    {
      title: "Our Values",
      content: "If you are passionate about construction and engineering and eager to grow in a challenging and opportunity-filled environment, join our team! Send your resume to engineering@viewco.com.hk for more information."
    },
    {
      title: "Apply Now",
      subtitle: "Job Application",
      content: "Please fill in the form below and our HR team will contact you as soon as possible."
    }
  ]
};
---
<Base title={t.pageTitle} description={t.heroSub}>
  <section class="page-hero" style="background:url(/Viewco-website-HKSP-photo-wmremove-1.jpeg) center/cover"><div style="position:absolute;inset:0;background:linear-gradient(135deg,#090a1aeb,#090a1ab3,#090a1ad9);z-index:1"></div><div class="container" style="position:relative;z-index:2"><h1>{t.heroTitle}</h1><p>{t.heroSub}</p></div></section>
  <section class="section"><div class="container"><div class="content-block">
    {t.sections.slice(0,3).map(s => (
      <div class="accordion-item">
        <h3 class="accordion-header" data-accordion-trigger>
          <span class="accordion-arrow">▶</span> {s.title}
          {s.subtitle && <small class="accordion-subtitle"> — {s.subtitle}</small>}
        </h3>
        <div class="accordion-body">
          {s.content && <p>{s.content}</p>}
          {s.subsections && s.subsections.map(sub => (
            <div style="margin-bottom:1rem">
              <h4 style="color:#181d4e;font-weight:500;margin-bottom:.25rem">{sub.title}</h4>
              <p style="margin:0">{sub.text}</p>
            </div>
          ))}
        </div>
      </div>
    ))}
    <div class="accordion-item">
      <h3 class="accordion-header open" data-accordion-trigger>
        <span class="accordion-arrow">▼</span> {t.sections[3].title}
        <small class="accordion-subtitle"> — {t.sections[3].subtitle}</small>
      </h3>
      <div class="accordion-body" style="display:block">
        <p>{t.sections[3].content}</p>
        <form class="contact-form" style="margin-top:1.5rem">
          <div class="form-group"><label>Position Applied For</label><input type="text" name="position" required placeholder="Position" /></div>
          <div class="form-group"><label>Name</label><input type="text" name="name" required placeholder="Name" /></div>
          <div class="form-group"><label>Email</label><input type="email" name="email" required placeholder="email@example.com" /></div>
          <div class="form-group"><label>Phone</label><input type="tel" name="phone" placeholder="+852" /></div>
          <div class="form-group"><label>Resume (PDF only)</label><input type="file" name="resume" accept=".pdf" /></div>
          <div class="form-group"><label><input type="checkbox" required /> I agree to the collection and storage of my submitted data (<a href="/en/privacy/">Privacy Policy & Disclaimer</a>)</label></div>
          <button type="submit" class="btn-submit">Submit Application</button>
        </form>
      </div>
    </div>
  </div></div></section>
  <style>
    .accordion-item { margin-bottom: .75rem; border: 1px solid #e0e0e0; border-radius: 6px; overflow: hidden; }
    .accordion-header { cursor: pointer; padding: 1rem 1.25rem; margin: 0; font-size: 1.1rem; font-weight: 500; color: #181d4e; background: #fafafa; display: flex; align-items: center; gap: .5rem; user-select: none; transition: background .2s; }
    .accordion-header:hover { background: #f0f0f0; }
    .accordion-header .accordion-subtitle { font-weight: 400; color: #888; font-size: .85rem; }
    .accordion-arrow { display: inline-block; transition: transform .25s; font-size: .75rem; width: 16px; text-align: center; flex-shrink: 0; }
    .accordion-header.open .accordion-arrow { transform: rotate(90deg); }
    .accordion-body { display: none; padding: 1rem 1.25rem 1.25rem 2.5rem; background: #fff; }
    .accordion-body p { margin-bottom: .75rem; line-height: 1.8; color: #444; }
    .contact-form { display: flex; flex-direction: column; gap: 1rem; max-width: 600px; }
    .form-group { display: flex; flex-direction: column; gap: .25rem; }
    .form-group label { font-size: .9rem; font-weight: 500; color: #333; }
    .form-group input[type="text"], .form-group input[type="email"], .form-group input[type="tel"] { padding: .7rem .9rem; border: 1px solid #ccc; border-radius: 4px; font-size: .95rem; }
    .form-group input[type="checkbox"] { width: auto; margin-right: .5rem; }
    .btn-submit { background: #fa4729; color: #fff; border: none; padding: .8rem 2rem; font-size: 1rem; font-weight: 500; border-radius: 4px; cursor: pointer; transition: opacity .2s; align-self: flex-start; }
    .btn-submit:hover { opacity: .8; }
    @media (max-width: 767px) { .accordion-body { padding-left: 1.5rem; } }
  </style>
  <script>
    document.addEventListener("DOMContentLoaded", () => {
      document.querySelectorAll("[data-accordion-trigger]").forEach(h => {
        h.addEventListener("click", () => {
          const b = h.nextElementSibling, o = b.style.display === "block";
          b.style.display = o ? "none" : "block";
          h.classList.toggle("open", !o);
          const a = h.querySelector(".accordion-arrow");
          if (a) a.textContent = o ? "▶" : "▼";
        });
      });
    });
  </script>
</Base>
